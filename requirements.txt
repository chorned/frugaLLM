# FrugaLLM 2.0 — Python Dependencies
# Install with: pip install -r requirements.txt

# Core: LiteLLM proxy server
litellm[proxy]>=1.40.0

# Validation: Gatekeeper Tier 2 Schema Validation
jsonschema>=4.20.0

# Optional: Langfuse telemetry
langfuse==2.60.10

# Optional: PostgreSQL spend logging (requires DATABASE_URL)
prisma>=0.13.0
asyncpg>=0.29.0

# Optional: Prometheus metrics
prometheus_client>=0.20.0
