# ═══════════════════════════════════════════════════════════════════════════════
# AUTO-GENERATED by FrugaLLM Dynamic Roster Sidecar — DO NOT EDIT MANUALLY
# This is an EXAMPLE of what the sidecar generates. The actual file is
# overwritten every 5 minutes based on the current OpenRouter free model pool.
# ═══════════════════════════════════════════════════════════════════════════════

model_list:
  # ── Balanced Chain ──
  - model_name: free_balanced
    litellm_params:
      model: openrouter/google/gemma-4-31b-it:free
      api_key: os.environ/OPENROUTER_API_KEY
      timeout: 300
      max_retries: 0

  - model_name: free_balanced_2
    litellm_params:
      model: openrouter/qwen/qwen3-coder:free
      api_key: os.environ/OPENROUTER_API_KEY
      timeout: 300
      max_retries: 0

  - model_name: free_balanced_backup
    litellm_params:
      model: ollama/llama3.2:latest
      api_base: http://127.0.0.1:11434
      timeout: 300
      max_retries: 0

  # ── Reasoning Chain ──
  - model_name: free_reasoning
    litellm_params:
      model: openrouter/qwen/qwen3-coder:free
      api_key: os.environ/OPENROUTER_API_KEY
      timeout: 300
      max_retries: 0

  - model_name: free_reasoning_backup
    litellm_params:
      model: ollama/llama3.2:latest
      api_base: http://127.0.0.1:11434
      timeout: 300
      max_retries: 0

router_settings:
  fallbacks:
    - {"auto": ["free_balanced"]}
    - {"reasoning": ["free_reasoning"]}
    - {"local": ["free_balanced"]}
    - {"free_balanced": ["free_balanced_2"]}
    - {"free_balanced_2": ["free_balanced_backup"]}
    - {"free_reasoning": ["free_reasoning_backup"]}
